# Jenkins Integration Example

This directory contains an example `Jenkinsfile` for integrating JFrog Vision analysis into a Jenkins pipeline.

See the `Jenkins_Integration-v5.26.md` file in the parent directory for full documentation.
